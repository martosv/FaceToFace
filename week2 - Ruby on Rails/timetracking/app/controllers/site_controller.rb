class SiteController < ApplicationController
	def home
		#render 'home' # render se puede suprimir, ya que el propio metodo te lleva a una dirección con el nombre del método
	end

	def contact
		#render 'contact'
	end
end
