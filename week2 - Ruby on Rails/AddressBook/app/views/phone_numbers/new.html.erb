<% if flash[:errors] %>
  <% flash[:errors].each do |error_msg| %>
    <li><%= error_msg %></li>
  <% end %>
<% end %>

<%= form_for [@contact, @phone_number] do |f| %>

  <h2 class="p5 top-5">Information</h2>

  <%= f.label :phone_number %>
  <%= f.text_field :phone_number %> <br>

  <%= f.submit 'Save' %>

<% end %>

  <p class="styleblack">
      <%= link_to 'Back to Contact', contact_path(@contact)%>
  </p>